services:
  router:
    build: .
    env_file: .env
    ports:
      - "8080:8080"
  ollama:
    image: ollama/ollama:latest
    profiles: ["local"]
    volumes:
      - ollama:/root/.ollama

volumes:
  ollama:
